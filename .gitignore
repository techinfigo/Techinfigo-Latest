# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Next.js build output and generated types
.next/
next-env.d.ts

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Next.js / TypeScript build output
# (.next/ and next-env.d.ts are already covered above)
out/
build/
*.tsbuildinfo

# Local environment files — never commit real IDs or secrets.
# .env.example is the documented template and stays tracked.
.env
.env.local
.env*.local
!.env.example

# Service-account keys. Credentials belong in .env.local, never in a tracked
# file — a committed key stays a live credential in the clone history forever.
firebase-key.json
*-key.json
*serviceAccount*.json
